<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TextExtractor" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/light_purple</item>
        <item name="colorPrimaryVariant">@color/light_purple</item>
        <item name="android:backgroundTint">@color/background</item>
        <item name="android:statusBarColor">@color/light_purple</item>
    </style>

    <style name="Theme.TextExtractor" parent="Base.Theme.TextExtractor" />
</resources>